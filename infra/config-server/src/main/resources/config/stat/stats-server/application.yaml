spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: false

  sql:
    init:
      mode: always

  datasource:
    url: jdbc:postgresql://localhost:6542/ewm-stats
    username: stat
    password: stat
    driver-class-name: org.postgresql.Driver